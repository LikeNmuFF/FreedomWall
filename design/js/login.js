// Animate card after typing
window.addEventListener("load", () => {
    setTimeout(() => {
        document.getElementById("loginCard").classList.add("show");
    }, 3000); // waits for typing animation to finish
});